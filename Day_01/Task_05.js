//  Declare a variable using let, assign it an initial value, reassign a new value. and log both values to the console

let name = "mohit";
console.log(`before reassign value:${name}`);
name = "sanjay";
console.log(`after reassign value:${name}`);

